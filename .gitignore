.DS_Store
_site/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock
node_modules/
.claude/settings.local.json
.claude/*.lock
og-image.old.png
# CF Portfolio Campaign — contains POC emails (PII)
docs/campaigns/cf-portfolio-eligible.json
docs/campaigns/cf-portfolio-pilot-emails.csv
# TypeScript build artifacts inside tools/
tools/*/dist/
# agentsfirst-radar moved to capitalthought/agentsfirst-radar (private companion repo).
# This .gitignore no longer needs the radar's state allowlist.

# og-card build cache (fetched favicons)
tools/og-card/.logo-cache/
