# Required env vars for {{PROJECT_NAME}}.
# Copy this file to .env and fill in real values. .env is gitignored.
# `npm run prep` checks that every var listed here is set.

# Example — replace with your real keys.
{{PROJECT_NAME_UPPER}}_API_KEY=
{{PROJECT_NAME_UPPER}}_API_URL=https://api.example.com
